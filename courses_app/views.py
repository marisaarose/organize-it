from django.shortcuts import render
from django.http import HttpResponse
#from courses_app.models import Instructor, Course,Course_Attachment,Course_Meeting, 
# Create your views here.

##def View_course(request):
    
 ##   course_list = Instructor.objects.order_by('instructor_id')
  ##  course_dict = {'instructor': course_list}
    
  ##  return render(request, 'courses_app/course-app.html', context = course_dicts)
##